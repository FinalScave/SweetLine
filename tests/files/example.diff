diff --git a/nginx/site.conf b/nginx/site.conf
index 8d3f2a1..a4b9c7f 100644
--- a/nginx/site.conf
+++ b/nginx/site.conf
@@ -1,14 +1,23 @@
 server {
-    listen 80;
-    server_name example.com;
+    listen 443 ssl http2;
+    server_name example.com www.example.com;
+
+    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains";
+    add_header X-Content-Type-Options "nosniff";
+    add_header Referrer-Policy "strict-origin-when-cross-origin";
     location / {
-        proxy_pass http://127.0.0.1:8080;
+        proxy_pass https://backend.internal:8443;
+        proxy_set_header Host $host;
+        proxy_set_header X-Forwarded-Proto $scheme;
+        proxy_set_header X-Request-Id $request_id;
     }
+
+    location /healthz {
+        return 200 "ok\n";
+    }
 }
diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..d51e0fe
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,17 @@
+# Ignore generated build outputs.
+/build/
+*.log
+!/build/keep.txt
+cache/**/
+docs/[A-Za-z]*/draft-?.md
+config/\#local.env
+secrets/\!shared.key
+terraform/.terraform/
+terraform/*.tfstate
+node_modules/
+.env
+!.env.example
+!.github/workflows/
+images/\[draft\].png
+notes/space\ name.txt
+assets/**/sprite-??.png
diff --git a/config/app.jsonc b/config/app.jsonc
index 194ef72..be7f203 100644
--- a/config/app.jsonc
+++ b/config/app.jsonc
@@ -1,10 +1,16 @@
 {
-  "name": "sweetline-demo",
-  "inlineStyle": false,
-  "theme": "light"
+  // Runtime settings
+  "name": "sweetline-demo",
+  "inlineStyle": true,
+  "theme": "night",
+  "homepage": "https://example.com/docs",
+  "servers": [
+    "https://api.example.com",
+    "https://cdn.example.com"
+  ]
 }
diff --git a/src/highlight/theme.css b/src/highlight/theme.css
similarity index 78%
rename from src/theme/default.css
rename to src/highlight/theme.css
index 16dc9a1..9b47c82 100644
--- a/src/theme/default.css
+++ b/src/highlight/theme.css
@@ -1,12 +1,18 @@
-.token-keyword { color: #6b5bff; }
-.token-string { color: #1c9a5f; }
+.token-keyword { color: #0f5bd8; }
+.token-string { color: #1c9a5f; }
 .token-comment { color: #7a8899; }
+@media (prefers-color-scheme: dark) {
+  .token-background { background: #111827; }
+}
+a.token-url:hover { text-decoration: underline; }
diff --git a/docs/guide.md b/docs/guide.md
deleted file mode 100644
index 3a9f1b2..0000000
--- a/docs/guide.md
+++ /dev/null
@@ -1,8 +0,0 @@
-# Guide
-
-Visit https://example.com/docs for more details.
-Use `sweetline --help` to list available commands.
-
-## Themes
-
-Choose the `night` theme for demos.
diff --git a/assets/logo.png b/assets/logo.png
new file mode 100644
index 0000000..4b825dc
Binary files /dev/null and b/assets/logo.png differ
diff --git a/scripts/bootstrap.sh b/scripts/bootstrap.sh
old mode 100644
new mode 100755
index 2d4c5e6..8f9a0b1
--- a/scripts/bootstrap.sh
+++ b/scripts/bootstrap.sh
@@ -1,7 +1,17 @@
 #!/usr/bin/env bash
-echo "Installing dependencies"
+set -euo pipefail
+echo "Installing dependencies"
+export APP_ENV=staging
+export APP_DOCS_URL="https://example.com/install"
 npm install
+npm run lint
+npm run test
+if [ -n "${CI:-}" ]; then
+  echo "Running in CI"
+fi
+printf 'done\n'
diff --git a/app/models/user.rb b/app/models/user.rb
index aa11bb2..cc33dd4 100644
--- a/app/models/user.rb
+++ b/app/models/user.rb
@@ -4,12 +4,18 @@ class User < ApplicationRecord
   validates :email, presence: true
 
   def admin?
-    role == "admin"
+    role.in?(%w[admin owner])
   end
 
-  def profile_url
-    "https://example.com/users/#{id}"
+  def profile_url(host: "https://example.com")
+    "#{host}/users/#{id}"
+  end
+
+  def display_name
+    nickname.presence || email.split("@").first
   end
 end
\ No newline at end of file
