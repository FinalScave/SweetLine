# Changelog

## Unreleased

- Add `tabSize` to the common `HighlightConfig` and pass it on Android, iOS, and JVM.

## 1.0.0

- Add `HighlightEngine.removeDocument(String)` on Android, iOS, and JVM targets.
- Add explicit `close()` support for native-backed engine, document, and analyzer objects.
